import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The dev badge sits exactly where the jar's bottom-left affordances go.
  devIndicators: false,
};

export default nextConfig;
